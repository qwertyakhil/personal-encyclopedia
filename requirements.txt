# Knowledge Pipeline — dependencies
# Install into the project virtualenv: python3.11 -m pip install -r requirements.txt
#
# Phase 0 (foundation)
pyyaml

# Phase 1 (YouTube + articles)
youtube-transcript-api==1.2.4
markitdown[youtube-transcription]==0.1.6
python-frontmatter==1.3.0

# Phase 2 (Instagram path) — uncomment when starting Phase 2
# yt-dlp
# faster-whisper

# Phase 5 (Watcher / RSS feeds) — uncomment when starting Phase 5
# feedparser
