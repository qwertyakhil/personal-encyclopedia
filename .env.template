# Knowledge Pipeline — Environment Variables
# Copy this to .env and fill in values. Never commit .env to git.

# Optional: capture auth token (for Phase 1 iOS Shortcut → Mac endpoint)
CAPTURE_TOKEN=your_secret_token_here
